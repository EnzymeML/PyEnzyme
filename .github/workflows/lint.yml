name: Ruff
on: [push, pull_request]
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: chartboost/ruff-action@v1
        with:
          src: './pyenzyme'
      - uses: chartboost/ruff-action@v1
        with:
          src: './tests'
